package date;

import java.time.Duration;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.Period;
import java.util.Scanner;

public class DemoDate {
public static void main(String[] args) {
	/*
	 * LocalDate ld=LocalDate.now(); System.out.println(ld);
	 * 
	 * LocalTime lt=LocalTime.now(); System.out.println(lt);
	 * 
	 * Period d=Period.between(LocalDate.parse("2024-07-01"), LocalDate.now());
	 * System.out.println(d);
	 * 
	 * Duration du=Duration.between(LocalTime.parse("17:00"),LocalTime.now());
	 * System.out.println(du);
	 * 
	 * LocalDate of = LocalDate.of(2022, 01, 01); System.out.println(of);
	 * 
	 * Duration dur=Duration.between(LocalTime.parse("17:00"),
	 * LocalTime.parse("17:30")); System.out.println(dur);
	 */
	
	/*
	 * System.out.println("Finding the age of a person by DOB"); Scanner sc=new
	 * Scanner(System.in);
	 * System.out.println("Enter the DOB in this Format yyyy-mm-dd"); String
	 * str=sc.next(); Period between = Period.between(LocalDate.parse(str),
	 * LocalDate.now()); System.out.println(between.plusYears(1));
	 * System.out.println(between.getYears()); Duration between2 =
	 * Duration.between(LocalTime.parse("17:00"), LocalTime.now());
	 * System.out.println(between2);
	 */
	
	
	
	
	
}
}
